#include <stdio.h>
void main()
{
    int a;
    printf("enter civil marks psst:no lieing");
    scanf("%d",&a);
    if(a>80)
    printf("Treat ice cream for little bandarr");
    if(a<=80)
    printf("its okay ree youve done well only");
}
